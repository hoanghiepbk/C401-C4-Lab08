# Day 08 Lab - RAG Pipeline
# Install: pip install -r requirements.txt

# === Core ===
python-dotenv>=1.0.0

# === LLM Providers (choose at least one) ===
openai>=1.0.0                    # Option A: OpenAI API
#
# Lưu ý: Code hiện tại dùng OpenAI. Nếu muốn Gemini, cần implement thêm trong rag_answer.py.

# === Vector Store ===
chromadb>=0.5.0                  # Vector database (local, no server needed)

# === Embeddings - choose one ===
# Option A: OpenAI Embeddings (dùng chung API key với LLM)
# (đã có trong openai package)

# Option B: Sentence Transformers (dùng cho rerank; embedding local chưa implement)
sentence-transformers>=2.7.0     # Model: paraphrase-multilingual-MiniLM-L12-v2

# === Sprint 3: Sparse/Hybrid Retrieval ===
rank-bm25>=0.2.2                 # BM25 keyword search

# === Sprint 3: Rerank ===
# sentence-transformers đã bao gồm CrossEncoder

# === Utils ===
tqdm>=4.66.0                     # Progress bar while indexing docs
